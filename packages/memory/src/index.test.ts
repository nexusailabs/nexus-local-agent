import { describe,expect,it } from 'vitest';import { mkdtempSync,rmSync } from 'node:fs';import { tmpdir } from 'node:os';import path from 'node:path';import { MemoryStore } from './index.js';
describe('MemoryStore',()=>{it('persists and searches memories',()=>{const dir=mkdtempSync(path.join(tmpdir(),'nexus-memory-'));const store=new MemoryStore(dir);store.store({kind:'workspace',namespace:'repo',content:'Qwen inference endpoint lives on the M5 node',importance:90});expect(store.search('Qwen',{namespace:'repo'})[0]?.content).toContain('M5');store.close();rmSync(dir,{recursive:true,force:true});});});
