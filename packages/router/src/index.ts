import type { ExecutionRequirements,ModelCapability,NodeCapability,NodeSpec,TaskKind } from '@nexus/protocol';
export type InferenceRoute={nodeId:string;modelId:string;score:number;reasons:string[]};export type ExecutionRoute={nodeId:string;score:number;reasons:string[]};export type StepRoute={inference:InferenceRoute;execution:ExecutionRoute};
const kindModelCaps:Record<TaskKind,ModelCapability[]>={plan:['reasoning','long-context'],code:['coding','tool-use'],shell:['tool-use'],build:['tool-use'],test:['tool-use','coding'],review:['review','reasoning','coding'],research:['reasoning','long-context'],general:['reasoning']};
const kindNodeCaps:Record<TaskKind,NodeCapability[]>={plan:['exec'],code:['exec','fs','git'],shell:['exec'],build:['exec'],test:['exec'],review:['exec'],research:['exec'],general:['exec']};const reachabilityScore={local:18,lan:12,wan:-8} as const;
export function routeInference(nodes:NodeSpec[],kind:TaskKind):InferenceRoute{const required=kindModelCaps[kind]??[],candidates=nodes.filter((node)=>node.capabilities.includes('inference')).flatMap((node)=>node.models.map((model)=>({node,model})));if(!candidates.length)throw new Error('No live inference models registered');const scored=candidates.map(({node,model})=>{const capScore=required.reduce((score,cap)=>score+(model.capabilities.includes(cap)?18:-14),0),score=capScore+model.qualityClass*.48+model.speedClass*.24-model.costClass*.04+reachabilityScore[node.reachability]+node.reliabilityClass*.08;return{nodeId:node.id,modelId:model.id,score,reasons:[`model-capabilities:${capScore}`,`quality:${model.qualityClass}`,`speed:${model.speedClass}`,`reachability:${node.reachability}`,`reliability:${node.reliabilityClass}`]};});scored.sort((a,b)=>b.score-a.score);return scored[0]!;}
const hasAll=(node:NodeSpec,caps:NodeCapability[])=>caps.every((cap)=>node.capabilities.includes(cap));
export function routeExecution(nodes:NodeSpec[],kind:TaskKind,requirements:ExecutionRequirements={requiredCapabilities:[],preferredCapabilities:[],avoidNodes:[]}):ExecutionRoute{const required=[...new Set([...kindNodeCaps[kind],...requirements.requiredCapabilities])],candidates=nodes.filter((node)=>!requirements.avoidNodes.includes(node.id)&&hasAll(node,required)&&(!requirements.platform||node.platform===requirements.platform)&&(!requirements.region||node.region===requirements.region));if(!candidates.length)throw new Error(`No live execution node satisfies kind=${kind} capabilities=${required.join(',')} platform=${requirements.platform??'*'} region=${requirements.region??'*'}`);const scored=candidates.map((node)=>{const preferred=requirements.preferredCapabilities.reduce((score,cap)=>score+(node.capabilities.includes(cap)?8:0),0);const score=node.executionClass*.45+node.reliabilityClass*.28+reachabilityScore[node.reachability]+preferred;return{nodeId:node.id,score,reasons:[`required:${required.join(',')||'none'}`,`preferred:${preferred}`,`execution:${node.executionClass}`,`reliability:${node.reliabilityClass}`,`reachability:${node.reachability}`,`region:${node.region??'unspecified'}`]};});scored.sort((a,b)=>b.score-a.score);return scored[0]!;}
export const routeStep=(nodes:NodeSpec[],kind:TaskKind,requirements?:ExecutionRequirements):StepRoute=>({inference:routeInference(nodes,kind),execution:routeExecution(nodes,kind,requirements)});export const routeTask=routeInference;
