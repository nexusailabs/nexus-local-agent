cluster:
  controlNodeId: mbp-m5-max-control
  heartbeatTtlMs: 45000
  # Normal operation is dynamic: node-daemons self-register and heartbeat.
  # Bootstrap nodes exist only for recovery/static lab scenarios.
  bootstrapNodes: []
